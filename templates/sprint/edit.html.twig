{% extends 'base.html.twig' %}

{% block title %}Modifier {{ sprint.name }}{% endblock %}

{% block body %}
    <div class="card wide">
        <h1>Modifier le sprint</h1>
        {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.goal) }}
            <div style="display:flex; gap:1rem">
                <div style="flex:1">{{ form_row(form.startDate) }}</div>
                <div style="flex:1">{{ form_row(form.endDate) }}</div>
            </div>
            <div class="form-actions">
                <button class="btn">Enregistrer</button>
                <a class="btn btn-secondary" href="{{ path('app_project_backlog', {id: sprint.project.id}) }}">Annuler</a>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
